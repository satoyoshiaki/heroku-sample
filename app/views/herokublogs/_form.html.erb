<%= form_with(model: herokublog, local: true) do |form| %>
  <% if herokublog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(herokublog.errors.count, "error") %> prohibited this herokublog from being saved:</h2>

      <ul>
      <% herokublog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :context %>
    <%= form.text_area :context %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
